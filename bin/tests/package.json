{
  "name": "hipay-enterprise-sdk-woocommerce",
  "version": "1.0.0",
  "dependencies": {
    "cypress": "^3.1.3"
  },
  "scripts": {
    "test:cypress": "$(npm bin)/cypress run",
    "cypress": "cypress open"
  }
}
